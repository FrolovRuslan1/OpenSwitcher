// If you read it you shold know that X11 is absolutely old shit please try support to develop new Window Maneger
/*!
	@file
	@brief This is the utilities header file of OpenSwitcher project.

	This file declares a variety of utilities for OpenSwitcher project such as
	message(), debug(), get_prefix(), append_char_to_string(), count_codepoints() and concat().
*/
#ifndef UTILITIES_H
#define UTILITIES_H

#include "common.h"
#include "functional.h"

extern char *get_prefix(const char *program);
extern int message(const char *text);
extern int debug(const char *text);
extern char* append_char_to_string(char* str, char c);
extern int count_codepoints(char* utf8_string);
extern char *concat(char *str1, char *str2);



#endif//UTILITIES_H